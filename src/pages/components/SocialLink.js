import styled from '@emotion/styled'

const SocialLink = styled.a`
    svg {
        margin: 5px;
        padding: 15px;
        width: 30px;
        height: 30px;
    }

    svg:hover {
        opacity: 0.7;
    }
`

export default SocialLink
